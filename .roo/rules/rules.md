GENERAL RULES:

- Never change unrelated code
- Prefer minimal, explicit solutions over clever ones
- Ask for clarification ONLY if requirements are missing
- Follow existing project structure and conventions
- Do not introduce new libraries unless explicitly approved
- Always explain WHY a decision is made (briefly)

CODING RULES:

- Write clean, readable, production-ready code
- Avoid overengineering
- Prefer vanilla JS / native APIs unless framework is already used
- Comment only when logic is non-obvious

WORKFLOW:

- If task is unclear, propose a plan first
- If task is large, break into steps before coding
- Do not refactor unless explicitly requested

OUTPUT FORMAT:

- Use markdown
- Show file paths when modifying files
- Explain changes after code
